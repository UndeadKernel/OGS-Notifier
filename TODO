
Detect logged-in state better
    - Fix the login form button
    - Show logged-in status when switching server in options 
    - Store username/password (pw encoded sha1)?

Fix flicker when popup loads for the first time

Add option to force refresh to right-click menu (invalidate timer and restart)
